#include <gtest/gtest.h>
#include "SimpleLinearNDInterpolator.h"

// 基本的なテストケース
TEST(SimpleLinearNDInterpolatorTest, BasicTest) {
    // テストが正常に動作することを確認
    EXPECT_TRUE(true);
}

int main(int argc, char **argv) {
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
